{
  "name": "masterzhang-platform",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Master Zhang — Multi-service property care platform for Markham & York Region. Moving, deep cleaning, junk removal, seasonal yard care & snow clearing.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "cf-typegen": "wrangler types",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.0",
    "@astrojs/sitemap": "^3.4.0",
    "@fontsource-variable/archivo": "^5.2.5",
    "@fontsource-variable/instrument-sans": "^5.2.5",
    "astro": "^5.12.0",
    "tailwindcss": "^4.1.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.20.0"
  }
}
